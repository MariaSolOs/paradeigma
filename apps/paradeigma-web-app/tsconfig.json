{
    "include": [
        "next-env.d.ts",
        "next.config.mjs",
        "**/*.ts",
        "**/*.tsx"
    ],
    "exclude": [
        "node_modules"
    ],
    "extends": "../../tsconfig.base.json",
    "compilerOptions": {
        "baseUrl": ".",
        "noEmit": true,

        // Needed for typings of dynamic local components.
        // TODO: Not sure if this is the right solution (?)
        "moduleResolution": "node"
    }
}